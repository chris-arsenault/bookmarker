# Backlog

Planned-but-not-built work. Each item is a positive assertion of future-state behavior.

## Capture surfaces

- Add browser extension capture for desktop workflows.
- Add Android offline queueing with retry once connectivity returns.
- Add bulk import from exported bookmark and watch-later files.
- Add a Gradle/JDK Android compile gate once the platform build standard exists.

## Enrichment

- Add official provider API credentials for richer YouTube, TikTok, Instagram, and generic OpenGraph metadata.
- Add provider embeds if the API later exposes provider-specific embed URLs safely.
- Add manual retry controls for failed archive snapshots.
- Add thumbnail replacement when a better source image becomes available.

## Library workflows

- Add saved search views for recurring filters.
- Add bulk tag editing for selected items.
- Add export of canonical URLs, notes, tags, and archive metadata.

## Platform operations

- Add application-level metrics for enrichment error rate and snapshot storage failures beyond the current Lambda CloudWatch alarms.
- Add deployed thumbnail-access smoke checks once an operator-owned fixture item exists.
